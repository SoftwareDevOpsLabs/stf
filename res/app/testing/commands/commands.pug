div.page-controller(ng-controller='CommandsCtrl')

  div.page-title 测试类型
  input.form-control.name-input(type='text',ng-model='test_name',list="groupList", ng-change="LoadTestGroup(test_name)")
  datalist(id="groupList")
    option(ng-repeat='template in templates', ng-value='template.group')


  div.page-title 测试场景
  input.form-control.name-input(type='text',ng-model='test_scenario',list="scenarioList",ng-change="LoadTestScenario(test_scenario)")
  datalist(id="scenarioList")
    option(ng-repeat='scenario in scenarios', ng-value='scenario.scenario')

    <!-- @hy 2017-06-04, add package name -->
  div.page-title 测试工具包名
  input.form-control.name-input(type='text',ng-model='test_package')

  div.page-title 测试工具执行环境
  button(type='button', ng-model='run_env', uib-btn-radio="'device'").btn.btn-sm.btn-default-outline 手机设备
  button(type='button', ng-model='run_env', uib-btn-radio="'server'").btn.btn-sm.btn-default-outline STF 服务器

  <!-- @hy 2017-05-08, add note below -->
  div.page-title 测试命令(*)
    p(ng-if='run_env=="server"') 设备的serial编号用{SN}替换, 包名用{PKG}替换 如 adb -s {SN} shell ls /data/data/{PKG}
    input.form-control(type='text',ng-model='test_command')

  div.page-title 可用设备列表(*)
  select-device-list(tracker='tracker',devices='devices')
  button.btn.btn-primary(ng-click='startTest()') 开始测试

  div.page-title 测试列表
  testing-column-list(columns='columns',on-stop='stopTest(this)').selectable


